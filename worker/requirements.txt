# OCRワーカーに必要なパッケージ
windows-capture
pygetwindow
pytesseract      # HP（数字）OCR
easyocr          # 名前（日本語）OCR
Pillow           # 画像前処理
numpy            # 画像配列処理

# 別途インストールが必要:
#   Tesseract本体（pytesseractは本体のラッパー）
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki からインストーラー入手
#            日本語データ(jpn)も忘れずに選択
